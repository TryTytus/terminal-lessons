# git add

Stage files for the next commit.

## Daily forms

- `git add <file>` - Stage one file.
- `git add .` - Stage new and modified files from current directory.
- `git add -u` - Stage changes to tracked files only.

## Example

```sh
git add README.md
```

## Practice note

Use `git status --short` to verify exactly what is staged.
