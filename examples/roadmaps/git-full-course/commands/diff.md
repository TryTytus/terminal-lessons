# git diff

Inspect line-level changes before committing.

## Daily forms

- `git diff` - Unstaged changes.
- `git diff --staged` - Staged changes.
- `git diff --stat` - Summary by file.

## Example

```sh
git diff --staged
```

## Practice note

Always inspect staged content before commit.
