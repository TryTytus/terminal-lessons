# git init

Create a new Git repository in the current directory.

## Daily forms

- `git init` - Initialize with default branch name.
- `git init -b main` - Initialize and set initial branch to `main`.

## Example

```sh
git init -b main
```

## Practice note

After init, run `git status` to confirm branch and clean state.
