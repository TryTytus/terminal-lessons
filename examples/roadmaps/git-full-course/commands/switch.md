# git switch

Move to another branch with modern branch-switch UX.

## Daily forms

- `git switch <branch>` - Switch to existing branch.
- `git switch -c <branch>` - Create and switch to new branch.

## Example

```sh
git switch -c feature/profile
```

## Practice note

Prefer `switch` for branch movement and `restore` for file recovery.
