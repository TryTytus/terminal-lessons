# alias

Define a shell shortcut for the current terminal session.

## Daily forms

- `alias name='command'` - Create a temporary alias.

## Example

```sh
alias ll="ls -l"
```

## Practice note

Create the requested output file from the command result, then run checks before moving on.
