# cat

Display whole files and number output lines.

## Daily forms

- `cat <file>` - View file contents.
- `cat -n <file>` - Number all output lines.

## Example

```sh
cat notes.txt > notes-copy.txt
```

## Practice note

Create the requested output file from the command result, then run checks before moving on.
