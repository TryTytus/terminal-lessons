# head

Show the first N lines of a file.

## Daily forms

- `head -n <count> <file>` - View the first N lines.

## Example

```sh
head -n 3 access.log > first-three.txt
```

## Practice note

Create the requested output file from the command result, then run checks before moving on.
