# history

Write recent commands from the interactive shell history.

## Daily forms

- `history` - View command history.

## Example

```sh
history > shell-history.txt
```

## Practice note

Create the requested output file from the command result, then run checks before moving on.
