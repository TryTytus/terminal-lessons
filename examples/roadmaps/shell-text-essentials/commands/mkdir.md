# mkdir

Create one directory or a whole missing parent path.

## Daily forms

- `mkdir <dir>` - Create a directory.
- `mkdir -p <path>` - Create missing parent directories.

## Example

```sh
mkdir reports
```

## Practice note

Create the requested output file from the command result, then run checks before moving on.
