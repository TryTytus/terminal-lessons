# nl

Add line numbers to text while preserving the original file.

## Daily forms

- `nl <file>` - Number lines of files.

## Example

```sh
nl tasks.txt > numbered.txt
```

## Practice note

Create the requested output file from the command result, then run checks before moving on.
