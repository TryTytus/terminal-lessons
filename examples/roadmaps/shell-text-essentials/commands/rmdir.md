# rmdir

Delete directories only when they are empty.

## Daily forms

- `rmdir <empty-dir>` - Remove an empty directory.

## Example

```sh
rmdir empty-box
```

## Practice note

Create the requested output file from the command result, then run checks before moving on.
