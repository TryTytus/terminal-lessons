# unalias

Delete a temporary alias from the current shell session.

## Daily forms

- `unalias name` - Remove an alias.

## Example

```sh
unalias note && echo "removed" > unalias-status.txt
```

## Practice note

Create the requested output file from the command result, then run checks before moving on.
