# uniq

Remove or count repeated adjacent lines after sorting when needed.

## Daily forms

- `uniq <file>` - Remove duplicate adjacent lines.
- `uniq -c <file>` - Count occurrences of adjacent lines.

## Example

```sh
uniq repeated.txt > unique.txt
```

## Practice note

Create the requested output file from the command result, then run checks before moving on.
